# A1413 Image

Using the same image proportions as the 2A0335+096 image which is ~19.2”.

The data was collected with channel averaging of 16, time averaging of 60s. 

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

# First self calibration

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(1, 'ea12')
```

- cconfig-p1
    - 29~31 drop < 30%, 16 in review.
    - Amp noise spw 21~31 up to ~0.5.
    - Phase calibration [75, -100].
- dconfig1-p1
    - Drop 10,22~24,26~31 less than 35% remaining.
    - Noise in microwave + ~9.4 Ghz.
    - Phase calibration [-50, 50] up to [-150, 150] at high freq.
- dconfig2-p1
    - Drop 10,23~31 less than 35% remaining.
    - Noise in microwave + ~9.4 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] at high freq.

```jsx
flagdata('cconfig-p1.ms', spw='29~31', flagbackup=False)
flagdata('dconfig1-p1.ms', spw='10,22~24,26~31', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='10,23~31', flagbackup=False)
flagdata('cconfig-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig1-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig2-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
```

# Second self calibration

Trying to get point source models. 2.6 uJy RMS. 

```jsx
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"],
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(2, 'ea12')
```

- cconfig-p2
    - 0,10,23~31 drop < 30%, 22 in review.
    - Amp noise spw 21~31 up to ~0.14.
    - Phase calibration [-50, 50x].
- dconfig1-p2
    - Drop 16,25 less than 35% remaining.
    - Not bad
    - Phase calibration [-20, 20] up to [-150, 100] at highest freq.
- dconfig2-p2
    - Drop 22 less than 35% remaining. 15,16 om review.
    - Not bad
    - Phase calibration [-30, 30] up to [-75, 50] at high freq.

```jsx
flagdata('cconfig-p2.ms', spw='0,10,23~31', flagbackup=False)
flagdata('dconfig1-p2.ms', spw='16,25', flagbackup=False)
flagdata('dconfig2-p2.ms', spw='22', flagbackup=False)
```

# Calibration 3

Previous looked good, add some small scales and retry with positive robust. RMS 5 uJy.

```jsx
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(3, 'ea12')
```

- cconfig-p3
    - 11,15~18,20~22 drop < 30%,
    - Amp noise is ok
    - Phase calibration [-20, 20].
- dconfig1-p3
    - 11,17,21 in review
    - Not bad
    - Phase calibration [-20, 20] up to [-40, 40] ~10 GHz.
- dconfig2-p3
    - 15,16,21, in review.
    - Not bad
    - Phase calibration [-10, 10] up to [-40, 40] at 10 GHz.

```jsx
flagdata('cconfig-p3.ms', spw='11,15~18,20~22', flagbackup=False)
```

# Calibration 4

Last looked very good, just need to see if we can get close to RMS since the residuals have a lot of real still. RMS ~2.6 uJy

```jsx
tclean(vis=["cconfig-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"],
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.01mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(4, 'ea12')
```

- cconfig-p4
    - 10~31 is bad, why is it like this.
    - Amp noise is ok
    - Phase calibration [-10, 10] (for good spw).
- dconfig1-p4
    - 11,15,17,20~21 in review
    - Not bad
    - Phase calibration [-20, 20] up to [-40, 40] ~10 GHz.
- dconfig2-p4
    - 15~16,21, in review.
    - Not bad
    - Phase calibration [-10, 10] up to [-40, 40] at 10 GHz.

```jsx
flagdata('cconfig-p4.ms', spw='10~31', flagbackup=False)
```

# Final Image

RMS was 2.6. Residuals show extended emission so dropping to 3x RMS.

```jsx
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"],
	imagename='image-p4',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.008mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```