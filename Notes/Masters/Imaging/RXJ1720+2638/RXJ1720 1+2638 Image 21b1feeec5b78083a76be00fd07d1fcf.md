# RXJ1720.1+2638 Image

Using the same image proportions as the 2A0335+096 image which is ~19.2”.

The data was collected with channel averaging of 16, time averaging of 60s. 

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	interactive=True, 
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

Centre is approximately 1e-4 Jy, above rest of noise.

# First self calibration

 Using ea12 in case the ea10 was half flagged.

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-init',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	interactive=True, 
	stokes='I', weighting='natural', robust=0.5, pbcor=False, 
	savemodel='modelcolumn')
```

```jsx
gaincal(vis="dconfig_2.ms", caltable=f'dconfig2.p0', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="dconfig_1.ms", caltable=f'dconfig1.p0', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="cconfig.ms", caltable=f'cconfig.p0', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
```

```jsx
applycal(vis="dconfig_2.ms", gaintable=['dconfig2.p0'])
split(vis=f'dconfig_2.ms', outputvis=f'dconfig2-p0.ms', 
	datacolumn='corrected')
applycal(vis="dconfig_1.ms", gaintable=['dconfig1.p0'])
split(vis=f'dconfig_1.ms', outputvis=f'dconfig1-p0.ms', 
	datacolumn='corrected')
applycal(vis="cconfig.ms", gaintable=['cconfig.p0'])
split(vis=f'cconfig.ms', outputvis=f'cconfig-p0.ms', 
	datacolumn='corrected')
```

cconfig: 0% → 36%

dconfig_1: 0% → 38%

dconfig_2: 0% → 31%

# Calibration 2

Doing better modelling than before. Interactive only.

```python
tclean(vis=["cconfig-p0.ms", "dconfig1-p0.ms", "dconfig2-p0.ms"], 
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=2000, gain=0.1, threshold='0.015mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	interactive=True, 
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	savemodel='modelcolumn')
```

Model is way better here. Using ea12 in case the ea10 was half flagged.

```jsx
gaincal(vis="dconfig2-p0.ms", caltable=f'dconfig2.p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="dconfig1-p0.ms", caltable=f'dconfig1.p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="cconfig-p0.ms", caltable=f'cconfig.p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
```

```jsx
applycal(vis="dconfig2-p0.ms", gaintable=['dconfig2.p1'])
split(vis=f'dconfig2-p0.ms', outputvis=f'dconfig2-p1.ms', 
	datacolumn='corrected')
applycal(vis="dconfig1-p0.ms", gaintable=['dconfig1.p1'])
split(vis=f'dconfig1-p0.ms', outputvis=f'dconfig1-p1.ms', 
	datacolumn='corrected')
applycal(vis="cconfig-p0.ms", gaintable=['cconfig.p1'])
split(vis=f'cconfig-p0.ms', outputvis=f'cconfig-p1.ms', 
	datacolumn='corrected')
```

cconfig: 25% → 28% (missing cals for 24, 28~31)

dconfig_1: 28% → 32% (missing cals for 10, 28~31)

dconfig_2: 25% → 25% (missing cals for 24, 29~31)

# Calibration 3

Going to 45 uJy to start, then clean deeper based on if model still looking nice. 

```jsx
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"], 
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=2000, gain=0.1, threshold='0.045mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	interactive=True, 
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	savemodel='modelcolumn')
```

```jsx
gaincal(vis="dconfig2-p1.ms", caltable=f'dconfig2.p2', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="dconfig1-p1.ms", caltable=f'dconfig1.p2', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
gaincal(vis="cconfig-p1.ms", caltable=f'cconfig.p2', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
```

```jsx
applycal(vis="dconfig2-p1.ms", gaintable=['dconfig2.p2'])
split(vis=f'dconfig2-p1.ms', outputvis=f'dconfig2-p2.ms', 
	datacolumn='corrected')
applycal(vis="dconfig1-p1.ms", gaintable=['dconfig1.p2'])
split(vis=f'dconfig1-p1.ms', outputvis=f'dconfig1-p2.ms', 
	datacolumn='corrected')
applycal(vis="cconfig-p1.ms", gaintable=['cconfig.p2'])
split(vis=f'cconfig-p1.ms', outputvis=f'cconfig-p2.ms', 
	datacolumn='corrected')
```

cconfig 12% → 14% (missing 22~31)

dconfig1 23% → 23% (missing 10, 24, 26, 28~31)

dconfig2 16% → 16% (missing 22, 24, 26, 28~31)

# Calibration 4

6e-6 Jy noise in image, clean down to 18uJy - 3 sigma (with mostly interactive)

```jsx
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=2000, gain=0.1, threshold='0.018mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	interactive=True, 
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	savemodel='modelcolumn')
```

# Calibration but with automasking

Using the default values to see what happens. Giving it 10000 iters for fun and profit.

- noisethreshold = 5
- sidelobethreshold = 1.25
- lownoisethreshold = 2
- minbeamfrac = 0.1
- negativethreshold = 0.0
- fastnoise = False

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2-auto',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.018mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Trying natural weighting to see if it removes the yuck noise. Nope

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2-auto-natural',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.018mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='natural', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Using briggs model to calibrate again, there is a small negative around right source, but -1e-7 magnitude, so hopefully small effect. 

cconfig 9% → 9% (missing 22-26, 28-31)

dconfig1 22% → 22% (missing 10, 23-25, 28-31)

dconfig2 24% → 25% (missing 10, 22-31)

# Calibration 3

```python
tclean(vis=["cconfig-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"], 
	imagename='image-p3-auto',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.018mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

rms is now 4.5 uJy. Still a good chunk in the residuals. Trying another selfcalibrate

cconfig 0% → 0% (missing 10, 22-31)

dconfig1 22% → 22% (missing 10, 23-25, 28-31)

dconfig1 25% → 26% (missing 10, 23-28-31)

# Calibration 4

Lowering bound to 3x 4.5 uJy ~14 uJy.

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.014mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

[https://www.notion.so](https://www.notion.so)

Looks good apart from the rhs point source which keeps getting negative values around it in the model. Can go slightly deeper with current mask, maybe 10 uJy.

## Tune attempt

Try increasing sidelobethreshold, decreasing noise threshold. (5e-6 from region RMS)

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto-tune1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.008mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=1.5, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Didn’t really improve things.

Clean deeper interactively on the first one 

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.014mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	interactive=True,
	savemodel='modelcolumn')
```

## Increase scales

Keep higher lobe threshold, add larger scale for minihalo structure.

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto-tune2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.014mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=1.5, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Looks great, still able to clean the centre, so interactively doing so to prevent external areas from being cleaned. The RMS is ~3.8 uJy in the surrounding residuals, while the minihalo still has 6 uJy. Using double surround RMS and interactively cleaning.

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto-tune2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.005mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	interactive=True,
	savemodel='modelcolumn')
```

Then applying self calibrate with this model and using result for good minihalo image

```python
self_calibrate(5, 'ea12')
```

## Trying to get a better p5 calibration

Restoring the flagging to pre self calibrated. Then assessing the SPW with `assess_spw`. Find that 

- cconfig has 10, 22~31 dead as expected (unflagged <1%).
    - 15 has low % unflagged (37%) which is below the relative drop threshold.
    - 16 is has 10% unflagged.
    - Dropping 10, 16, 22~31 expects 0.7% loss, adding 15 is 3.1% loss in unflagged data. This seems acceptable.
- dconfig1 has 10, 13~14, 22~31 below absolute floor
    - Proposed drop is in data is 2%
- dconfig2 has 10, 22~31 as below absolute floor.
    - Proposed drop is 0.1%

```python
flagdata("cconfig-p4.ms", spw='10,15~16,22~31')
flagdata("dconfig1-p4.ms", spw='10,13~14,22~31')
flagdata("dconfig2-p4.ms", spw='10,22~31')
```

### Run the imaging

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4-auto-scales',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.015mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=1.5, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```python
self_calibrate(5, 'ea12')
```

```python
tclean(vis=["cconfig-p5.ms", "dconfig1-p5.ms", "dconfig2-p5.ms"], 
	imagename='image-p5',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=10000, gain=0.1, threshold='0.015mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=1.5, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```