# A2626 Image

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

# Calibration 1

RMS was 31 uJy. Making point source model.

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(1, 'ea12')
```

```jsx
gaincal(vis='cconfig_2.ms', caltable='cconfig2-p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
applycal(vis='cconfig_2.ms', gaintable=['cconfig2-p1'])
split(vis='cconfig_2.ms', outputvis=f'cconfig2-p1.ms', datacolumn='corrected')
```

- cconfig-p1
    - All fine
    - Spikes up to 0.4, noise >10 GHz up to 0.4
    - Phase calibration [-20, 20].
- cconfig2-p1
    - All fine
    - Small spikes up to 0.25
    - Phase calibration [-50, 50].
- dconfig1-p1
    - 30,31 in review
    - Noise 10.7-12 GHz, up to ~1.6, spikes 9.4 GHz. Clip to 0.2
    - Phase calibration [-20, 20]
- dconfig2-p1
    - 10 in review
    - Noise 10.7-12 GHz, up to ~3 spikes 9.4, 10 GHz. Clip to 0.2.
    - Phase calibration [-50, 50].

```jsx
flagdata('dconfig1-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig2-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
```

# Calibration 2

Getting good point source model. Other double cconfig that has similar psf has trouble with sidelobes so upping constraint here. Seems to have negative points, throwing at check_flux the dconfig ~20% brighter so separate AGN fits probably a good idea. 

Adding some small scales to better approximate, robust -0.5 since the cconfig is throwing a bunch of negatives for some reason. Scaling the models since AGN seems quite variable, use image fit flux scaled to individual image flux.

```jsx
tclean(vis=["cconfig-p1.ms", "cconfig2-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"],
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```python
self_calibrate(2, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p2
    - All fine
    - Spikes up to 0.4, noise >10 GHz up to 0.4
    - Phase calibration [-5, 5].
- cconfig2-p2
    - All fine
    - Small spikes up to 0.25
    - Phase calibration [-5, 5].
- dconfig1-p2
    - 30,31 in review
    
    - Noise 10.7-12 GHz, up to ~1.6, spikes 9.4 GHz. Clip to 0.2
    - Phase calibration [-2, 2], up to [-20, 20] at review spw
- dconfig2-p2
    - 10 in review
    - Noise 10.7-12 GHz, up to ~3 spikes 9.4, 10 GHz. Clip to 0.2.
    - Phase calibration [-5, 5]., spike to [-20, 20] at 9.4 GHz.

# Calibration 3

Last was mid, lots of negatives around AGN, trying deeper clean to see what comes out

```python
tclean(vis=["cconfig-p2.ms", "cconfig2-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=-0.5,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```