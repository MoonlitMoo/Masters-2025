# MS1455+2232 Image

Using the same image proportions as the 2A0335+096 image which is ~19.2”.

The data was collected with channel averaging of 16, time averaging of 60s. 

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	interactive=True, 
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

# Calibration 1

Shouldn’t use ea10, ea09, ea14, ea16 as reference for phase calibration. 

Clipping extremes

```python
flagdata('cconfig.ms', mode='clip', clipminmax=(0, 0.1))
flagdata('dconfig_1.ms', mode='clip', clipminmax=(0, 0.1))
flagdata('dconfig_2.ms', mode='clip', clipminmax=(0, 0.1))
```

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=2000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, 
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(1, 'ea12')
```

Assess spw results

- cconfig-p1
    - All fine
    - Amp noise spw in microwave and ~10 GHz
    - Phase calibration [-20, 20] up to [-40, 40] at high freq.
- dconfig1-p1
    - Drop 30,31 almost all flagged., 22,24 in review
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-20, 20] up to [-40, 40] at high freq.
- dconfig2-p1
    - 10 in review.
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-20, 20].

```python
flagdata('dconfig1-p1.ms', spw='30~31')
```

# Calibration 2

Doing same as above + scales. Sidelobes are being picked up in bright south source.

```jsx
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"], 
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.05mJy', scales=[0, 2, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Better model, will calibrate from this since it doesn’t love going deeper.

```python
self_calibrate(2, 'ea12')
```

Assess spw results

- cconfig-p2
    - All fine
    - Amp noise spw in microwave and ~10 GHz
    - Phase calibration [-4, 4] up to [-20, 20] at high freq.
- dconfig1-p2
    - 22, 24 in review
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-5, 5] up to [-10, 10] at high freq.
- dconfig2-p2
    - 10 in review.
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-5, 5].

# Calibration 3

RMS is 3 uJy

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.02mJy', scales=[0, 2, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Assess spw results

- cconfig-p3
    - All fine
    - Amp noise spw in microwave and ~10 GHz
    - Phase calibration [-2, 2] up to [-10, 10] at high freq.
- dconfig1-p3
    - 22, 24 in review
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-2, 2]
- dconfig2-p3
    - 10 in review.
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-2, 2].

# Calibration 4

The S2 quasar is pretty variable, so modelling this separately per dataset for the self calibration. Not a great image, but calibrated well enough that we can continue to minihalo imaging. Main problem is S point source which is doing some interesting things.

```python
from casatasks import tclean

datasets = ['cconfig-p3', 'dconfig1-p3', 'dconfig2-p3']
OUTPUT_DIR = "calibration4"
full_model = "image-p3"

# Create the full model
tclean(vis=[f"{d}.ms" for d in datasets], 
	imagename=full_model,
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.02mJy', scales=[0, 2, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
)
	
# Create the individual models
for ms in datasets:
    tclean(vis=f"{ms}.ms",
        imagename=f'{OUTPUT_DIR}/{ms}',
        imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
        specmode='mfs', niter=20000, gain=0.1,
        deconvolver='mtmfs', pblimit=-1.e-6,
        threshold='0.012mJy', scales=[0, 2, 4, 8],
        stokes='I', weighting='briggs', robust=0.5, pbcor=False,
        usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
        lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False
    )

# Create the custom models
nterms = 2
MODEL_DIR = "calibration4/models"
variable_region = "circle[[14h57m10.80s, 22d18m44.71s], 5arcsec]"

if os.path.exists(MODEL_DIR):
    os.system(f'rm -r {MODEL_DIR}')
os.makedirs(MODEL_DIR)

for ms in datasets:
    base_models = [f'{full_model}.model.tt{i}' for i in range(nterms)]
    fitted_models = [f'{OUTPUT_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    new_models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]

    for b, f, n in zip(base_models, fitted_models, new_models):
        # Copy base model
        os.system(f'cp -r {b} {n}')
        # Open the new model and insert the fitted region
        ia.open(n)
        ia.insert(infile=f, region=head_tail_region)
        ia.close()

# Apply model to the datasets
for ms in datasets:
    models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    ft(vis=f'{ms}.ms', nterms=nterms, model=models, usescratch=True)

```

```python
self_calibrate(4, 'ea12')
```

Assess spw results

- cconfig-p4
    - All fine
    - Amp noise spw in microwave and ~10 GHz
    - Phase calibration [-2, 2] up to [-10, 10] at high freq.
- dconfig1-p4
    - 22, 24 in review
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-2, 2]
- dconfig2-p4
    - 10 in review.
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-2, 2], to -4 at higher freq.

# Calibration 5

```python
from casatasks import tclean

datasets = ['cconfig-p4', 'dconfig1-p4', 'dconfig2-p4']
OUTPUT_DIR = "calibration5"
full_model = "image-p4"

# Create the full model
tclean(vis=[f"{d}.ms" for d in datasets], 
	imagename=full_model,
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.02mJy', scales=[0, 2, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
)
	
# Create the individual models
for ms in datasets:
    tclean(vis=f"{ms}.ms",
        imagename=f'{OUTPUT_DIR}/{ms}',
        imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
        specmode='mfs', niter=20000, gain=0.1,
        deconvolver='mtmfs', pblimit=-1.e-6,
        threshold='0.012mJy', scales=[0, 2, 4, 8],
        stokes='I', weighting='briggs', robust=0.5, pbcor=False,
        usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
        lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False
    )

# Create the custom models
nterms = 2
MODEL_DIR = f"{OUTPUT_DIR}/models"
variable_region = "circle[[14h57m10.80s, 22d18m44.71s], 5arcsec]"

if os.path.exists(MODEL_DIR):
    os.system(f'rm -r {MODEL_DIR}')
os.makedirs(MODEL_DIR)

for ms in datasets:
    base_models = [f'{full_model}.model.tt{i}' for i in range(nterms)]
    fitted_models = [f'{OUTPUT_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    new_models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]

    for b, f, n in zip(base_models, fitted_models, new_models):
        # Copy base model
        os.system(f'cp -r {b} {n}')
        # Open the new model and insert the fitted region
        ia.open(n)
        ia.insert(infile=f, region=head_tail_region)
        ia.close()

# Apply model to the datasets
for ms in datasets:
    models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    ft(vis=f'{ms}.ms', nterms=nterms, model=models, usescratch=True)

```

```python
self_calibrate(5, 'ea12')
```

Assess spw results

- cconfig-p5
    - All fine
    - Amp noise spw in microwave and ~10 GHz
    - Phase calibration < [-2, 2]
- dconfig1-p5
    - 22, 24 in review
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-0.5, 0.5]
- dconfig2-p5
    - 10 in review.
    - Spikes at 9.4 GHz, noise in microwave.
    - Phase calibration [-0.5, 1]