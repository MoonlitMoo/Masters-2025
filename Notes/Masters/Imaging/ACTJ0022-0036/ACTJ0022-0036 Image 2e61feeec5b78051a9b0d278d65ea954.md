# ACTJ0022-0036 Image

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

# Calibration 1

Trying to get a good point source model first.

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(1, 'ea12')
```

```jsx
gaincal(vis='cconfig_2.ms', caltable='cconfig2.p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
applycal(vis='cconfig_2.ms', gaintable=['cconfig2.p1'])
split(vis='cconfig_2.ms', outputvis=f'cconfig2-p1.ms', datacolumn='corrected')
```

- cconfig-p1
    - Drop 30. 29 in review
    - Amp noise spw 16~31 up to ~0.8
    - Phase calibration [-50, 50].

- cconfig2-p1
    - Drop 30,31. 29 in review
    - Amp noise spw 21~31 up to ~0.7
    - Phase calibration [-50, 50].
- dconfig1-p1
    - Drop 23~24,26~31.
    - Noise 10.7-12 GHz, up to ~35, spikes 9.4, 10 GHz.
    - Phase calibration [-50, 50], up to 150 at high freq.
- dconfig2-p1
    - Drop 28, 29. 24,26,27 in review
    - Noise 10.7-12 GHz, up to ~12 spikes 9.4, 10 GHz.
    - Phase calibration [-30, 30], up to [-150, 150] at high freq

```jsx
flagdata('cconfig-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('cconfig2-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig1-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig2-p1.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('cconfig-p1.ms', spw='30', flagbackup=False)
flagdata('cconfig2-p1.ms', spw='30~31', flagbackup=False)
flagdata('dconfig1-p1.ms', spw='23,24,26~31', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='28~29', flagbackup=False)
```

# Calibration 2

```jsx
tclean(vis=["cconfig-p1.ms", "cconfig2-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"],
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(2, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p2
    - Drop 29, 25,28 in review.
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4, 8.3 GHz.
    - Phase calibration [-20, 20], up to [-75, 75] at high freq.
- cconfig2-p2
    - Drop 29 < 30%.
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4.
    - Phase calibration [-20, 20].
- dconfig1-p2
    - 22 in review.
    - Noise 10.7-12 GHz, up to ~0.2, noise 8.5-9.8 GHz.
    - Phase calibration [-30, 30], up to 150 at high freq.
- dconfig2-p2
    - Drop 28, 29. 24,26,27 in review (forgot to drop last cal)
    - Noise 10.7-12 GHz, up to ~0.2 spikes 9.4, 10 GHz.
    - Phase calibration [-10, 10], up to [-150, 150] > 11 GHz.

```jsx
flagdata('cconfig-p1.ms', spw='29', flagbackup=False)
flagdata('cconfig2-p1.ms', spw='29', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='28~29', flagbackup=False)
```

# Calibration 3

Looks good, add small scales + ssb, increasing slt. Going to positive robust. Using higher threshold as negative points are rocking up.

Might need to do WNW point individually. 

```jsx
tclean(vis=["cconfig-p2.ms", "cconfig2-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(3, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p3
    - Drop 29. 26, 28 in review
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4, 8.3 GHz.
    - Phase calibration [-15, 15] up to [-125, 75] at high freq
- cconfig2-p3
    - Drop 29
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4.
    - Phase calibration [-5, 5].
- dconfig1-p3
    - 10, 22, 25 in review
    - Noise 10.7-12 GHz, up to ~0.2, noise 8.5-9.8 GHz.
    - Phase calibration [-5, 5]
- dconfig2-p3
    - Drop 28~29. 23,24, 26,27 in review.
    - Noise 10.7-12 GHz, up to ~0.2 spikes 9.4, 10 GHz.
    - Phase calibration [-10, 10], up to [-30, 40] > 10.7 GHz.

(Flagged wrong ms last time)

```jsx
flagdata('cconfig-p1.ms', spw='29', flagbackup=False)
flagdata('cconfig2-p1.ms', spw='29', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='28~29', flagbackup=False)
```

# Calibration 4

Last image looks good. Going to lower threshold. RMS was 2.2 uJy

```jsx
tclean(vis=["cconfig-p3.ms", "cconfig2-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"],
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(4, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p4
    - Drop 28. 25,26 in review
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4, 8.3 GHz.
    - Phase calibration [-5, 5] up to [-40, 40] at ~11.2 GHz
- cconfig2-p4
    - Fine
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4.
    - Phase calibration [-2, 2], up to [-5, 5] at 11 GHz.
- dconfig1-p3
    - 10, 22, 25 in review
    - Noise 10.7-12 GHz, up to ~0.2, noise 8.5-9.8 GHz.
    - Phase calibration [-1, 1], [-10, 10] at 9.4 GHz.
- dconfig2-p3
    - 23,24, 26,27 in review.
    - Noise 10.7-12 GHz, up to ~0.2 spikes 9.4, 10 GHz.
    - Phase calibration [-5, 5], up to [-10, 10] > 10.7 GHz.

# Final Image

Looks good, see if we can get close to RMS. Last RMS was 2.1 uJy.

It’s ok, the WNW point probably needs subtracting individually alongside minihalo.

```jsx
tclean(vis=["cconfig-p4.ms", "cconfig2-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"],
	imagename='image-p4',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.01mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```