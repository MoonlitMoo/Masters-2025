# RXJ2129+0005 Image

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='natural',robust=0.5,pbcor=False)
```

# Calibration 1

Trying to get a good point source model first.

```jsx
tclean(vis=["cconfig.ms", "cconfig_2.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(1, 'ea12')
```

```jsx
gaincal(vis='cconfig_2.ms', caltable='cconfig2-p1', 
	solint='inf', refant='ea12', gaintype='G', calmode='p', solnorm=False)
applycal(vis='cconfig_2.ms', gaintable=['cconfig2-p1'])
split(vis='cconfig_2.ms', outputvis=f'cconfig2-p1.ms', datacolumn='corrected')
```

- cconfig-p1
    - 29, 30 in review.
    - Amp noise spw 21~31 up to ~1, lot of general noise as well. Clip 0.4
    - Phase calibration [-100, 100]
- cconfig2-p1
    - 31 in review
    - Amp noise spw 21~31 up to ~0.7, spikes 9.4. Clip 0.4
    - Phase calibration [-50, 50].
- dconfig1-p1
    - 23, 26, 29 in review
    - Noise 10.7-12 GHz, up to ~12, noise 9.4 GHz ~2. Clip 0.4
    - Phase calibration [-30, 30], up to 150 at high freq.
- dconfig2-p1
    - Drop 22,24~26,28~31 as below 35%. 23,27 in review.
    - Noise 10.7-12 GHz, up to ~40 spikes 9.4, 10 GHz. Clip 0.4
    - Phase calibration [-10, 10], up to [-150, 150] > 10.7 GHz.

```jsx
flagdata('cconfig-p1.ms', mode='clip', clipminmax=[0, 0.4], flagbackup=False)
flagdata('cconfig2-p1.ms', mode='clip', clipminmax=[0, 0.4], flagbackup=False)
flagdata('dconfig1-p1.ms', mode='clip', clipminmax=[0, 0.4], flagbackup=False)
flagdata('dconfig2-p1.ms', mode='clip', clipminmax=[0, 0.4], flagbackup=False)
flagdata('dconfig2-p1.ms', spw='22,24~26,28~31', flagbackup=False)
```

# Calibration 2

Looking alright, lowering threshold and keeping negative robust for point source modelling. Upping slt since the 2x cconfigs are having trouble here.

```jsx
tclean(vis=["cconfig-p1.ms", "cconfig2-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"],
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(2, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p2
    - 25,29,30 in review
    - Amp noise spw 21~31 up to ~0.4, lot of general noise as well. Clip to 0.2
    - Phase calibration [-25, 25], many spikes to [-50, 50]
- cconfig2-p2
    - 31 in review
    - Amp noise spw 21~31 up to ~0.4, spikes 9.4. Clip to 0.2
    - Phase calibration [-10, 10], spikes to [-20, 60] at high freq.
- dconfig1-p2
    - Drop 29. 23, 26 in review
    - Noise 10.7-12 GHz, up to ~12, noise 9.4 GHz ~2. Clip 0.2
    - Phase calibration [-10, 10], up to [-60, 80] at high freq.
- dconfig2-p2
    - Drop 27. 10, 23 in review.
    - Noise 10.7-12 GHz, up to ~40 spikes 9.4, 10 GHz. Clip 0.2
    - Phase calibration [-10, 10], up to [-50, 50] in review spw.

```jsx
flagdata('cconfig-p2.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('cconfig2-p2.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig1-p2.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig2-p2.ms', mode='clip', clipminmax=[0, 0.2], flagbackup=False)
flagdata('dconfig1-p2.ms', spw='29', flagbackup=False)
flagdata('dconfig2-p2.ms', spw='27', flagbackup=False)
```

# Calibration 3

Looking good, adding scales, moving to positive robust. Last RMS was 3.36 uJy.

```jsx
tclean(vis=["cconfig-p2.ms", "cconfig2-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(3, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p3
    - 25,28,29,30 in review
    - Amp noise spw 21~31 up to ~0.2, lot of general noise as well ~0.2.
    - Phase calibration [-20, 20], some spikes to [-40, 40], inconsistent as hell
- cconfig2-p3
    - Drop 31
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4.
    - Phase calibration [-5, 5], spikes to [-80, 40] at 31.
- dconfig1-p3
    - 23, 26 in review
    - Noise 10.7-12 GHz, up to ~12, noise 9.4 GHz ~0.2
    - Phase calibration [-2, 2], up to [-20, 40] at freq >10.7 GHz.
- dconfig2-p3
    - 10, 23 in review.
    - Noise 10.7-12 GHz, up to ~0.2 spikes 9.4, 10 GHz.
    - Phase calibration [-2, 2], up to [-40, 40] in review spw. Flag spw 10,23 since bad

```jsx
flagdata('cconfig2-p3.ms', spw='31', flagbackup=False)
flagdata('dconfig2-p3.ms', spw='10,23', flagbackup=False)
```

# Calibration 4

There was one negative pixel last time <threshold. Adding larger scales and lowering threshold. Lasr RMS was 1.9 uJy. The cconfig data is almost 40% lower than the d config, with small variation there too.

```jsx
tclean(vis=["cconfig-p3.ms", "cconfig2-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"],
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```python
self_calibrate(4, 'ea12', prefixes=['cconfig', 'cconfig2', 'dconfig1', 'dconfig2'])
```

- cconfig-p4
    - Drop 30. 25,28,29 in review
    - Amp noise spw 21~31 up to ~0.2, lot of general noise as well ~0.2.
    - Phase calibration [-20, 20], inconsistent.
- cconfig2-p4
    - All fine
    - Amp noise spw 21~31 up to ~0.2, spikes 9.4.
    - Phase calibration [-2, 2], spikes to [-20, 15] at spw 31
- dconfig1-p4
    - 23, 26 in review
    - Noise 10.7-12 GHz, up to ~12, noise 9.4 GHz ~0.2
    - Phase calibration [-1, 1], up to [-20, 20] at freq >10.7 GHz.
- dconfig2-p4
    - Fine
    - Noise 10.7-12 GHz, up to ~0.2 spikes 9.4, 10 GHz.
    - Phase calibration [-1, 1]

```python
flagdata('cconfig-p4.ms', spw='30', flagbackup=False)
```

# Final image

See if we can get close to RMS (2 uJy) with this

```python
tclean(vis=["cconfig-p4.ms", "cconfig2-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"],
	imagename='image-p4',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.01mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

## Trying with first cconfig gone?

```python
tclean(vis=["cconfig2-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"],
	imagename='image-p4-2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.01mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4, 8], smallscalebias=0.5,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```