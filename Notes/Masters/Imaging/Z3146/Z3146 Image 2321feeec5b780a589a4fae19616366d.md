# Z3146 Image

Using the same image proportions as the 2A0335+096 image which is ~19.2”.

The data was collected with channel averaging of 16, time averaging of 60s. 

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "dconfig1.ms", "dconfig2.ms"], 
        imagename='image-dirty',
        imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
        specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
        deconvolver='mtmfs', pblimit=-1.e-6,
        stokes='I', weighting='briggs',robust=0.5,pbcor=False
)
```

Possible external sources not captured North and South.

# First self calibration

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	interactive=True, 
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	savemodel='modelcolumn'
)
```

```jsx
self_calibrate(1, 'ea12')
```

- cconfig-p1
    - All fine.
    - Amp noise spw 21~31 up to ~0.8.
    - Phase calibration [-20, 20] up to [-50, 50] at high freq.
- dconfig1-p1
    - Drop 28,29 less than 35% remaining.
    - High noise 10.7-12 GHz, up to ~7.
    - Phase calibration [-20, 20] up to [-50, 50] at high freq.
- dconfig2-p1
    - drop 29 less than 35% remaining. 23,24,27 low compared to remaining
    - High noise 10.7-12 GHz, up to ~10.
    - Phase calibration [-20, 20]

```jsx
flagdata('dconfig1-p1.ms', spw='28~29', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='29', flagbackup=False)
flagdata('cconfig-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig1-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig2-p1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
```

# Calibration 2

```jsx
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"], 
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=2000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False, 
	savemodel='modelcolumn')
```

```jsx
self_calibrate(2, 'ea12')
```

- cconfig-p2
    - Drop 30~31.
    - Amp noise spw 21~31 up to ~0.15
    - Phase calibration [-10, 10] up to [-50, 50] at highest freq.
- dconfig1-p2
    - Drop 24 in review
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-10, 10], spike of [-20, 20] at 11 GHz
- dconfig2-p2
    - 22~24,27~28 in review
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-20, 20]

# Calibration 3

Try negative robust, lower RMS 4 uJy.

```jsx
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=2000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8],
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False
	savemodel='modelcolumn')
```

```jsx
self_calibrate(3, 'ea12')
```

One negative chunk in near the centre, but good overall.

- cconfig-p3
    - Drop 30~31. 29 in review. Clip to 0.08
    - Amp noise spw 21~31 up to ~0.15
    - Phase calibration [-5, 5] up in spw 30/31 that I forgot to drop.
- dconfig1-p3
    - 24 in review. Clip to 0.06
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-2.5, 2.5]
- dconfig2-p3
    - drop 23. 22,24,27~28 in review. Clip to 0.06
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-2.5, 2.5]

```jsx
flagdata('cconfig-p3.ms', spw='30~31', flagbackup=False)
flagdata('dconfig2-p3.ms', spw='23', flagbackup=False)
flagdata('cconfig-p3.ms', mode='clip', clipminmax=[0, 0.08], flagbackup=False)
flagdata('dconfig1-p3.ms', mode='clip', clipminmax=[0, 0.06], flagbackup=False)
flagdata('dconfig2-p3.ms', mode='clip', clipminmax=[0, 0.06], flagbackup=False)
```

# Calibration 4

Switch back to positive robust. 4.3 uJy RMS, keeping threshold since we have trouble fitting. Remove the largest scale. Upping sidelobethreshold.

```jsx
tclean(vis=["cconfig-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"],
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=2000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(4, 'ea12')
```

- cconfig-p4
    - 29 in review
    - Amp noise spw 21~31 up to ~0.15
    - Phase calibration [-5, 5] up to [-10, 10] at higher freq
- dconfig1-p4
    - 24 in review
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-1, 1] up to [-20, 20] in higher freq
- dconfig2-p4
    - 22,24,26~28 in review
    - High noise 10.7-12 GHz, up to ~0.15.
    - Phase calibration [-1, 1] up to [-15, 15] at higher freq.

# Calibration 5

The previous model looks good, but the residuals are still all over the place. Seeing if we can now clean deeper with this. RMS was 2.8 uJy. 0.01 didn’t work, picked up every sidelobe. Also increasing slt.

```jsx
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"],
	imagename='image-p4',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=2000, gain=0.1, threshold='0.015mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=4,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

Check point flux. The dconfig1 dataset is 25% lower than the others. See if we can treat AGN separately. The two AGN are merged in the dconfig data, so replacing both of these sources in the self calibration with region `agn_region = "circle[[10h23m45.05s, 4d10m39.73s], 10arcsec]"` .