# 2A0335+096 Image attempt 2

# Calibration 1

Just a generic high threshold point model. 

```jsx

tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"],
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

There is a bunch of noise, so clipping prior to the self calibration

```jsx
flagdata('cconfig.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig_1.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
flagdata('dconfig_2.ms', mode='clip', clipminmax=[0, 0.15], flagbackup=False)
```

```jsx
self_calibrate(1, 'ea12')
```

- cconfig-p1
    - Fine
    - Amp noise spw 21~31 up to ~0.15, spike at 9.4 GHz. Clip to 0.08.
    - Phase calibration [-40, 40].
- dconfig1-p1
    - Drop 10,22~31 < 35%.
    - Noise 10.7-12 GHz, up to ~0.15, spikes 9.4, 10 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] at high freq.
- dconfig2-p1
    - Drop 10, 22~31 < 35%. 11 in review.
    - Noise 10.7-12 GHz, up to ~0.15, spike at 9.5 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] at high freq.

```jsx
flagdata('cconfig-p1.ms', mode='clip', clipminmax=[0, 0.08], flagbackup=False)
flagdata('dconfig1-p1.ms', spw='10,22~31', flagbackup=False)
flagdata('dconfig2-p1.ms', spw='10,22~31', flagbackup=False)
```

# Calibration 2

Trying to get a good point source model. See if we can split to model AGN separately as one of the datasets has a flux ~20% different. Using agn region `circle[[3h38m40.55s, 9d58m11.8s], 5arcsec]`. Scaling in these regions to try and match agn flux for modelling.

```jsx
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"],
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(2, 'ea12')
```

- cconfig-p2
    - 10, 31 in review
    - Amp noise spw 21~31 up to ~0.15, spike at 9.4 GHz. Clip to 0.08.
    - Phase calibration [-40, 40].
- dconfig1-p2
    - Drop 21
    - Noise 10.7-12 GHz, up to ~0.15, spikes 9.4, 10 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] ~9 GHz.
- dconfig2-p2
    - Drop 11,15,16. 14 in review.
    - Noise 10.7-12 GHz, up to ~0.15, spike at 9.5 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] at some points

```jsx
flagdata('dconfig1-p2.ms', spw='21', flagbackup=False)
flagdata('dconfig2-p2.ms', spw='11,15~16', flagbackup=False)
```

# Calibration 3

The cconfig had a negative spot for some reason, dconfig looked fine. Swapping to positive robust, lowering threshold. Last residual was 4.5 uJy.

```jsx
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"],
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 2, 4],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```jsx
self_calibrate(3, 'ea12')
```

- cconfig-p3
    - 10, 31 in review
    - Amp noise spw 21~31 up to ~0.08, spike at 9.4 GHz. Clip to 0.04?
    - Phase calibration [-40, 40].
- dconfig1-p3
    - 0, 6, 16 in review.
    - Noise 10.7-12 GHz, up to ~0.15, spikes 9.4, 10 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] ~9 GHz.
- dconfig2-p3
    - Drop 14 in review. 12,13,17,18 in review
    - Noise 10.7-12 GHz, up to ~0.15, spike at 9.5 GHz.
    - Phase calibration [-50, 50] up to [-150, 150] at some points

```jsx
flagdata('cconfig-p3.ms', mode='clip', clipminmax=[0, 0.04], flagbackup=False)
```

# Calibration 4

Didn’t get the larger scales, so dropping threshold slightly + adding larger scales. The c config data still has negatives for some reason, but d config looks reasonable.

```jsx
tclean(vis=["cconfig-p3.ms", "dconfig1-p3.ms", "dconfig3-p2.ms"],
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
	specmode='mfs', niter=20000, gain=0.1, threshold='0.02mJy',
	deconvolver='mtmfs', pblimit=-1.e-6, scales=[0, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=2.5,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```