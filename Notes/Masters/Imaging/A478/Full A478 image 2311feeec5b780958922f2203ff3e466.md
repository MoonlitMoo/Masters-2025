# Full A478 image

Using the same image proportions as the 2A0335+096 image which is ~19.2”.

The data was collected with channel averaging of 16, time averaging of 60s. 

# Dirty Image

```jsx
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-dirty',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=0, gain=0.1, threshold='1.0mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	interactive=True,
	stokes='I', weighting='briggs',robust=0.5,pbcor=False)
```

# First self calibration

Clean down the 2 brightest points ~0.3 mJy

```python
tclean(vis=["cconfig.ms", "dconfig_1.ms", "dconfig_2.ms"], 
	imagename='image-p0',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.1mJy',
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False,
	savemodel='modelcolumn')
```

```python
self_calibrate(1, 'ea10')
```

Assess spw results

- cconfig-p1
    - All fine.
    - Amp noise spw 21~31 up to ~2.
    - Phase calibration ~[-10, 20] w/ spikes to +60 for spw 29-31
- dconfig1-p1
    - All fine
    - Amp noise at spw 21:3 to ~8
    - Phase calibration ~[-50, 50]
- dconfig2-p1
    - All fine
    - Amp noise at spw 21:3 to ~5
    - Phase calibration ~[-20, 40] with shift for 30,31

# Second calibration

Noise is 5e-6 Jy, this to get all point sources, no scales. Using threshold 3x sigma.

```python
tclean(vis=["cconfig-p1.ms", "dconfig1-p1.ms", "dconfig2-p1.ms"], 
	imagename='image-p1',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.05mJy', scales=[0, 2, 4, 8],
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```

Not bad, increase sidelobethreshold slightly more, deepen threshold, possibly look at smallscalebias. 

```python
self_calibrate(2, ea10)
```

Assess spw results

- cconfig-p2
    - All fine.
    - Amp noise spw 21~31 up to ~2.
    - Phase calibration ~[-10, 20] w/ slight widening propto frequency.
- dconfig1-p2
    - All fine, some in review
    - Amp noise at spw 21:3 to ~8
    - Phase calibration ~[-10, +10] widens to pm 20 in microwave with some more extreme points.
- dconfig2-p2
    - All fine
    - Amp noise at spw 21:3 to ~5
    - Phase calibration ~[-10, 15]

# Calibration 3

RMS is 4e-6, lower threshold and swapping to negative robust since it’s having a hard time in general. 

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.015mJy', scales=[0, 2, 4, 8],
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```

There’s a bit of head-tail missing, so interactive masking that region

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.015mJy', scales=[0, 2, 4, 8],
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	interactive=True)
```

Then cleanup to check

```python
tclean(vis=["cconfig-p2.ms", "dconfig1-p2.ms", "dconfig2-p2.ms"], 
	imagename='image-p2',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, threshold='0.015mJy', scales=[0, 2, 4, 8],
	deconvolver='mtmfs', pblimit=-1.e-6,
	stokes='I', weighting='briggs', robust=-0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```

Self calibration

```python
self_calibrate(3, 'ea10')
```

Assess spw results, no change from previous.

- cconfig-p3 phase calibration ~[-5, 10], slight offshoot for one antenna (ea24)
- dconfig1-p3 phase calibration ~[-5, 5] with microwave larger range ~pm 10 and some noisy points.
- dconfig2-p3 phase calibration ~[-10, 5], wider at low freq, thins to ~pm 2.5

# Calibration 4

Back to positive robust, trying to get decent image now. Skipping automask since it seems convinced that we should limit masking to some radius from the centre.

RMS is 2e-6, adding larger scale for minihalo

```python
tclean(vis=["cconfig-p3.ms", "dconfig1-p3.ms", "dconfig2-p3.ms"], 
	imagename='image-p3',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.008mJy', scales=[0, 2, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	interactive=True)
```

Looks alright, still negative impression around left head-tail, but close to noise. Consider flagging dconfig microwave and see if better.

- cconfig-p4
    - All fine.
    - Amp noise spw 21~31 up to ~2.
    - Phase calibration ~[-0.5, 0.5]
- dconfig1-p4
    - All okay, one in drop but barely, leaving in.
    - Amp noise at spw 21:3 to ~8
    - Phase calibration ~[-1, 1], microwave looks comparatively bad with spikes to 40.
- dconfig2-p4
    - All fine
    - Amp noise at spw 21:3 to ~5
    - Phase calibration ~[-1, 1], with 29, 31 looking noisy.

# Image 4

Flagging 26+ of dconfig1, 29,31 of dconfig2 and seeing if we get anything nicer.

```python
flagdata('dconfig1-p4.ms', spw='26~31')
flagdata('dconfig2-p4.ms', spw='29,31')
```

Then cleaning

```python
tclean(vis=["cconfig-p4.ms", "dconfig1-p4.ms", "dconfig2-p4.ms"], 
	imagename='image-p4',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.008mJy', scales=[0, 2, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	interactive=True)
```

# Calibration 5

Since the head-tail is seemingly quite variable, making individual models per dataset and splicing into the full model. Also applying the clipping to 0.1, since it doesn’t affect that much data and nicely cleans the amplitude spectra.

```python
flagdata('cconfig-p4.ms', mode='clip', clipminmax=(0, 0.1))
flagdata('dconfig1-p4.ms', mode='clip', clipminmax=(0, 0.1))
flagdata('dconfig2-p4.ms', mode='clip', clipminmax=(0, 0.1))
```

RMS of cconfig is 4 uJy, should use 12 uJy threshold to make sure automask and fits don’t wind up with weird models. Adding slight bias to smaller models. 

```python
for ms in datasets:
    tclean(vis=f"{ms}.ms",
        imagename=f'{OUTPUT_DIR}/{ms}',
        imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
        specmode='mfs', niter=20000, gain=0.1,
        deconvolver='mtmfs', pblimit=-1.e-6,
        threshold='0.012mJy', scales=[0, 2, 4, 8], 
        stokes='I', weighting='briggs', robust=0.5, pbcor=False,
        usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
        lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False
    )
```

Then we just recreate splice the models with the fourth calibration model to keep only the head-tail model for each one. Apply the models and self-calibrate

```python
datasets = ['cconfig-p4', 'dconfig1-p4', 'dconfig2-p4']
OUTPUT_DIR = "head_tail_models"

# Create the individual models
for ms in datasets:
    tclean(vis=f"{ms}.ms",
        imagename=f'{OUTPUT_DIR}/{ms}',
        imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'],
        specmode='mfs', niter=20000, gain=0.1,
        deconvolver='mtmfs', pblimit=-1.e-6,
        threshold='0.012mJy', scales=[0, 2, 4, 8],
        stokes='I', weighting='briggs', robust=0.5, pbcor=False,
        usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
        lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False
    )

# Create the custom models
nterms = 2
base_model = "image-p4"
MODEL_DIR = "custom_models"
head_tail_region = "ellipse[[4h13m36.61s, 10d28m23.96s], [20arcsec, 40arcsec], 30deg]"

if os.path.exists(MODEL_DIR):
    os.system(f'rm -r {MODEL_DIR}')
os.makedirs(MODEL_DIR)

for ms in datasets:
    base_models = [f'{base_model}.model.tt{i}' for i in range(nterms)]
    fitted_models = [f'{OUTPUT_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    new_models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]

    for b, f, n in zip(base_models, fitted_models, new_models):
        # Copy base model
        os.system(f'cp -r {b} {n}')
        # Open the new model and insert the fitted region
        ia.open(n)
        ia.insert(infile=f, region=head_tail_region)
        ia.close()

# Apply model to the datasets
for ms in datasets:
    models = [f'{MODEL_DIR}/{ms}.model.tt{i}' for i in range(nterms)]
    ft(vis=f'{ms}.ms', nterms=nterms, model=models, usescratch=True)

```

```python
self_calibrate(5, 'ea10')
```

Assess spw results are the same, phase calibrations are up to 20 degrees.

# Calibration 5

```python
tclean(vis=["cconfig-p5.ms", "dconfig1-p5.ms", "dconfig2-p5.ms"], 
	imagename='image-p5',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.008mJy', scales=[0, 2, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```

Not a huge amount of difference, cleaning down to 6 uJy (3x sigma) to see.

```python
self_calibrate(6, 'ea10')
```

```python
flagdata('cconfig-p6.ms', mode='clip', clipminmax=(0, 0.05))
flagdata('dconfig1-p6.ms', mode='clip', clipminmax=(0, 0.05))
flagdata('dconfig2-p6.ms', mode='clip', clipminmax=(0, 0.05))
```

Running self-calibration again to check convergence. 

- cconfig-p6
    - All fine in flagged amounts.
    - Amp noise higher in microwave, small spikes in weather. Nothing terrible though.
    - Phase calibration ~[-2, 2]
- dconfig1-p6
    - Remaining are fine (dropped 26~31)
    - Amp noise past 10 GHz.
    - Phase calibration ~[-2, 2], couple weird points spws >22
- dconfig2-p6
    - All fine
    - Some noise in microwave, but fine.
    - Phase calibration ~[-5, 5] at 8 GHz, down to ~[-2, 2] at 10 GHz

# Final image

```python
tclean(vis=["cconfig-p6.ms", "dconfig1-p6.ms", "dconfig2-p6.ms"], 
	imagename='image-p6',
	imsize=[2304, 2304], cell=['0.5arcsec','0.5arcsec'], 
	specmode='mfs', niter=20000, gain=0.1, 
	deconvolver='mtmfs', pblimit=-1.e-6,
	threshold='0.008mJy', scales=[0, 2, 4, 8, 16],
	stokes='I', weighting='briggs', robust=0.5, pbcor=False,
	usemask='auto-multithresh', noisethreshold=5, sidelobethreshold=1.25,
	lownoisethreshold=2, minbeamfrac=0.1, negativethreshold=0.0, fastnoise=False)
```

Looks alright, using for minihalo imaging.