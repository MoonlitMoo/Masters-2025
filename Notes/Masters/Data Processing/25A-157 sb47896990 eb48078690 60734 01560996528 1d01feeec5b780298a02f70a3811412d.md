# 25A-157.sb47896990.eb48078690.60734.01560996528

[Keep polarisation for RFI flagging?](25A-157%20sb47896990%20eb48078690%2060734%2001560996528/Keep%20polarisation%20for%20RFI%20flagging%201d01feeec5b7800da8d4c5b4684a6bb1.md)

[Manual Calibration](25A-157%20sb47896990%20eb48078690%2060734%2001560996528/Manual%20Calibration%2020d1feeec5b7807f9a81d33fbdfe21ad.md)

First we split the X band data into a trimmed dataset. We don’t need the C-band. We keep the polarised data since it can assist in flagging RFI.

```python
split(vis=msname, outputvis=f'trim.ms', spw='16~47', datacolumn='data')
rm -r 25A-157.sb47896990.eb48078690.60734.01560996528.ms
mv trim.ms 25A-157.sb47896990.eb48078690.60734.01560996528.ms
```

🚩 **Saved flag as initial_flagging**

The ea10 antenna was experiencing issues, so flagging it here. 

```python
flagdata(vis=msname, antenna='ea10', flagbackup=False)
```

🚩 **Saved as base_flagging**

# Pre-calibration checks

Check spectra to prepare aoflagger scripts.

```python
flagcmd(vis=msname, inpmode='table', reason='any', action='plot', 
	plotfile='plots/baseflagging.png')
plotants(vis=msname, figfile='plots/antenna_position.png')

check_flagging(msname)
plotms(vis=msname, field=p_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/primary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field=s_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/secondary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field="2A0335+096", xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='120', correlation='LL,RR', plotfile='plots/science_spw_amp.png')
```

# Pipeline part 1

```python
context = h_init()
context.set_state('ProjectSummary', 'observatory', 'Karl G. Jansky Very Large Array')
context.set_state('ProjectSummary', 'telescope', 'EVLA')
try:
    hifv_importdata(vis=['25A-157.sb47896990.eb48078690.60734.01560996528.ms'])
    hifv_flagdata(hm_tbuff='1.5int', fracspw=0.01, intents='*POINTING*,*FOCUS*,*ATMOSPHERE*,*SIDEBAND_RATIO*, *UNKNOWN*, *SYSTEM_CONFIGURATION*, *UNSPECIFIED#UNSPECIFIED*')
    hifv_vlasetjy()
    hifv_priorcals(show_tec_maps=False)
    hifv_aoflagger(flag_target='"0137+331=3C48"', aoflagger_file='0137+331=3C48.lua')
    hifv_testBPdcals()
    hifv_semiFinalBPdcals()
    hifv_aoflagger(flag_target='J0321+1221', aoflagger_file='J0321+1221.lua')
    hifv_solint()
    hifv_fluxboot()
finally:
    h_save()
```

## Checks

```python
check_flagging(msname)
plotms(vis=msname, field=p_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/primary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field=s_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/secondary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field="2A0335+096", xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='120', correlation='LL,RR', plotfile='plots/science_spw_amp.png')
```

- Saved under ‘midcalibration’ folder
- Delay looks fine
- Gain phase looks consistent, no concerning jumps.
- Bandpass looks alright, some weird phase chunks, but overall is ok.

Primary calibrator looks great, no need for additional flagging. Secondary calibrator still needs some work, flagging percentage low enough we can be more strict.

![field_J0321+1221_spw_flagging.png](25A-157%20sb47896990%20eb48078690%2060734%2001560996528/field_J03211221_spw_flagging.png)

# Pipeline part 2

```python
# This CASA pipescript is meant for use with CASA 6.6.1 and pipeline 2024.1.0.8
context = h_init()
context.set_state('ProjectSummary', 'observatory', 'Karl G. Jansky Very Large Array')
context.set_state('ProjectSummary', 'telescope', 'EVLA')
try:
    hifv_importdata(vis=['25A-157.sb47896990.eb48078690.60734.01560996528.ms'])
    hifv_flagdata(hm_tbuff='1.5int', fracspw=0.01, intents='*POINTING*,*FOCUS*,*ATMOSPHERE*,*SIDEBAND_RATIO*, *UNKNOWN*, *SYSTEM_CONFIGURATION*, *UNSPECIFIED#UNSPECIFIED*')
    hifv_vlasetjy()
    hifv_priorcals(show_tec_maps=False)
    hifv_aoflagger(flag_target='"0137+331=3C48"', aoflagger_file='0137+331=3C48.lua')
    hifv_testBPdcals()
    hifv_semiFinalBPdcals()
    hifv_aoflagger(flag_target='J0321+1221', aoflagger_file='J0321+1221.lua')
    hifv_solint()
    hifv_fluxboot()
    hifv_aoflagger(flag_target='"0137+331=3C48"', aoflagger_file='0137+331=3C48-corrected.lua', use_corrected=True)
    hifv_aoflagger(flag_target='J0321+1221', aoflagger_file='J0321+1221-corrected.lua', use_corrected=True)
    hifv_finalcals()
    hifv_applycals()
    hifv_aoflagger(flag_target='2A0335+096', aoflagger_file='2A0335+096.lua', use_corrected=True)
    hifv_statwt()
    hifv_plotsummary()
    hifv_exportdata()
finally:
    h_save()
```

## Checks

- Delay, bandpass look fine.
- Short phase has a few concerning phase jumps, but it’s only in SPW 30 so I’ll let it slide.
- Same deal with the final phase and amp, but it’s pretty small and only SPW 30. Also still no idea what its coloured by, even by staring at the data.
- Flagging looks fine as well, maximum without overflagging. Spectra looks alright but not perfect.

```python
check_flagging(msname)
plotms(vis=msname, field=p_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/primary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field=s_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/secondary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field="2A0335+096", xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='120', correlation='LL,RR', plotfile='plots/science_spw_amp.png')
```

![science_spw_amp.png](25A-157%20sb47896990%20eb48078690%2060734%2001560996528/science_spw_amp.png)

![field_2A0335+096_spw_flagging.png](25A-157%20sb47896990%20eb48078690%2060734%2001560996528/field_2A0335096_spw_flagging.png)

Saved under finalcalibration

# Split image data

```python
split(vis=msname,
      outputvis='dconfig_1.ms',
      field='2A0335+096', correlation="LL,RR",
      datacolumn='corrected',
      width=16, timebin='60s',
      keepflags=False)
```