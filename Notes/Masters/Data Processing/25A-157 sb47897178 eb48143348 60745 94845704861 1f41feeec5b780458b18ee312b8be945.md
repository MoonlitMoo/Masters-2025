# 25A-157.sb47897178.eb48143348.60745.94845704861

```python
split(vis=msname, outputvis='trim.ms', spw='16~47', datacolumn='data')
rm -r 25A-157.sb47897178.eb48143348.60745.94845704861.ms
mv trim.ms 25A-157.sb47897178.eb48143348.60745.94845704861.ms
```

🚩Saved as **initial_flagging**

Observation notes

- ea10 A1C1 has a DTS hardware issue missed by the pipeline.
- ea17 A1C1 and ea21 were flagged by the pipeline for hardware issues.
- ea24 A2C2 has a hardware issue at the beginning of the observation.

17, 21 preflagged. Adding ea10 A1C1. ~~Adding ea24 in first 3 scans since I don’t know how long they are talking about~~. The `ea24` antenna still looks weird in scan 34 of the primary calibrator, so flagging the entire scan

```python
flagdata(vis=msname, antenna='ea10', spw='0~15', flagbackup=False)
flagdata(vis=msname, antenna='ea24', spw='16~31', flagbackup=False)
```

🚩Saved as **base_flagging**

# Pre-calibration checks

```python
flagcmd(vis=msname, inpmode='table', reason='any', action='plot', 
	plotfile='plots/baseflagging.png')
plotants(vis=msname, figfile='plots/antenna_position.png')
```

```python
check_flagging(msname)
plotms(vis=msname, field=p_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/primary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field=s_calibrator, xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='60', correlation='LL,RR', plotfile='plots/secondary_spw_amp.png', plotrange=[0,0,0,1])
plotms(vis=msname, field="A478", xaxis='freq', yaxis='amp', coloraxis='spw', 
	avgtime='120', correlation='LL,RR', plotfile='plots/science_spw_amp.png')
```

# Run pipeline part 1

```python
# This CASA pipescript is meant for use with CASA 6.6.1 and pipeline 2024.1.0.8
context = h_init()
context.set_state('ProjectSummary', 'observatory', 'Karl G. Jansky Very Large Array')
context.set_state('ProjectSummary', 'telescope', 'EVLA')
try:
    hifv_importdata(vis=['25A-157.sb47897178.eb48143348.60745.94845704861.ms'])
    hifv_flagdata(hm_tbuff='1.5int', fracspw=0.01, intents='*POINTING*,*FOCUS*,*ATMOSPHERE*,*SIDEBAND_RATIO*, *UNKNOWN*, *SYSTEM_CONFIGURATION*, *UNSPECIFIED#UNSPECIFIED*')
    hifv_vlasetjy()
    hifv_priorcals(show_tec_maps=False)
    hifv_aoflagger(flag_target='0137+331=3C48', aoflagger_file='0137+331=3C48.lua')
    hifv_testBPdcals()
    hifv_semiFinalBPdcals()
    hifv_aoflagger(flag_target='0', aoflagger_file='J0424+0204.lua')
    hifv_solint()
    hifv_fluxboot()
 finally:
	 h_save()
```

Then figure out the aoflagger setup for the corrected data. 

## Checks

- Delay: Looks fine, most ~4-6 ns, max is 14 ns
- Gain phase: Looks smooth, no jumps.
- Bandpass amp: Looks mostly okay, e13, ea15, ea27 have the correlation difference in A2C2, but not really concerned. Nothing looks bad.
- Bandpass phase: Couple outlying points, but nothing extreme. Well contained in +/- 30 degrees.

The flagging looks good, primary calibrator probably doesn’t need a second calibrated pass. ~~Secondary still needs some work from a calibrated pass~~. Secondary isn’t getting a second pass since it killed off most of the data (even at light values), which killed the final phase calibration in spw 28~31. 

The fluxboot looks clean, very little noise for the secondary calibrator even with the noisy microwave interference. 

# Run pipeline part 2

```python
context = h_init()
context.set_state('ProjectSummary', 'observatory', 'Karl G. Jansky Very Large Array')
context.set_state('ProjectSummary', 'telescope', 'EVLA')
try:
    hifv_importdata(vis=['25A-157.sb47897178.eb48143348.60745.94845704861.ms'])
    hifv_flagdata(hm_tbuff='1.5int', fracspw=0.01, intents='*POINTING*,*FOCUS*,*ATMOSPHERE*,*SIDEBAND_RATIO*, *UNKNOWN*, *SYSTEM_CONFIGURATION*, *UNSPECIFIED#UNSPECIFIED*')
    hifv_vlasetjy()
    hifv_priorcals(show_tec_maps=False)
    hifv_aoflagger(flag_target='0137+331=3C48', aoflagger_file='0137+331=3C48.lua')
    hifv_testBPdcals()
    hifv_semiFinalBPdcals()
    hifv_aoflagger(flag_target='0', aoflagger_file='J0424+0204.lua')
    hifv_solint()
    hifv_fluxboot()
    hifv_finalcals()
    hifv_applycals()
    hifv_aoflagger(flag_target='1', aoflagger_file='A478.lua', use_corrected=True)
    hifv_statwt()
    hifv_plotsummary()
 finally:
	 h_save()
```

## Checks

- Delay, gain, bandpass still look ok
- Flagging went high in spw 30, hitting 90% for the microwave band after the calibration.
- Since the calibration looks ok barring few points in spw 30 where there is approximately 0 data, I left it as is.
- Since fields look good enough on flagging to check imaging.

![field_J0424+0204_spw_flagging.png](25A-157%20sb47897178%20eb48143348%2060745%2094845704861/field_J04240204_spw_flagging.png)

![field_A478_spw_flagging.png](25A-157%20sb47897178%20eb48143348%2060745%2094845704861/field_A478_spw_flagging.png)

![field_A478_antenna_flagging.png](25A-157%20sb47897178%20eb48143348%2060745%2094845704861/field_A478_antenna_flagging.png)

# Imaging split

```python
split(vis=msname, outputvis='dconfig_2.ms', field='A478', correlation='LL,RR', 
	width=16, timebin='60s', datacolumn='corrected', keepflags=False)
```