# RXJ2129+0129
This is self-calibrated and has a flux estimate.

The flux estimate uses a slightly different mask since I didn't source-subtract everything, mostly because I didn't realise there was a previous mask until I was almost done with the thesis. Trying to add extra source subtraction wasn't immediately doable since it would mean I'd have to change some things from the other clusters since it wasn't sensitive enough to fully delete the nearby source.

Folder structure should be standard.